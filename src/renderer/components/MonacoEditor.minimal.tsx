// Temporary legacy stub retained only because deletion attempts failed in automated patch flow.
// Replaced with a no-op component to remove console noise and test markup.
import React from 'react';

const MonacoEditor: React.FC = () => null;

export default MonacoEditor;
